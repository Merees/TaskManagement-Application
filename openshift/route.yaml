apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: taskflow
  labels:
    app: taskflow
spec:
  to:
    kind: Service
    name: taskflow
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

# Made with Bob
